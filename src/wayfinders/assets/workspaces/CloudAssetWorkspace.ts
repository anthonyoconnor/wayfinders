import type { CloudAssetWorkspaceModule } from "./AssetWorkspace";

export const CLOUD_ASSET_WORKSPACE: Readonly<CloudAssetWorkspaceModule> = Object.freeze({
  id: "clouds",
  label: "Clouds",
  kind: "cloud-preview",
});
