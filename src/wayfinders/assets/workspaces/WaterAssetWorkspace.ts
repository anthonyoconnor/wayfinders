import type { WaterAssetWorkspaceModule } from "./AssetWorkspace";

export const WATER_ASSET_WORKSPACE = Object.freeze({
  id: "water",
  label: "Water",
  kind: "water-preview",
} satisfies WaterAssetWorkspaceModule);
