import type { AchievementIconAssetWorkspaceModule } from "./AssetWorkspace";

export const ACHIEVEMENT_ICON_ASSET_WORKSPACE = Object.freeze({
  id: "icons",
  label: "Icons",
  kind: "achievement-icons-preview",
} satisfies AchievementIconAssetWorkspaceModule);
